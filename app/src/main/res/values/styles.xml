<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle" tools:ignore="NewApi">@style/SheraActionBar</item>
        <item name="actionBarStyle">@style/SheraActionBar</item>
        <item name="android:colorBackground">@color/white</item>
    </style>

    <style name="SheraActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="background">@color/light_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
